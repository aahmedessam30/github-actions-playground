name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome Message
        run: echo "Weclome to our first Github Actions Workflow!"
